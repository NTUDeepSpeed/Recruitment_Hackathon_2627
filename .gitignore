.DS_Store
.vscode/
.idea/

# Build output — the site is published to gh-pages, never committed here.
_site/
.deploy/
__pycache__/
*.pyc
.venv/

# Track-branch artefacts that survive a `git checkout main` in a shared
# working tree. Nothing here belongs on this branch.
simulator/
race_ws/build/
race_ws/install/
race_ws/log/
results/*.json
